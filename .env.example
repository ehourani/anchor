# Supabase — public values only (Project Settings → API in the Supabase dashboard).
# The anon key is safe to ship: every table is guarded by Row-Level Security.
# NEVER put the service-role key here or in any VITE_-prefixed variable.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
