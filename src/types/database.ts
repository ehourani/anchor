// Placeholder until the schema is pushed and types are generated:
//   supabase gen types typescript --linked > src/types/database.ts
// Regenerate this file after every schema change so queries stay type-safe.
export type Database = any
